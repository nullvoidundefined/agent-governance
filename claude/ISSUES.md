# Issues

Deferred P2/P3 work for the `~/.claude` rule system, per R-802/R-601. One line per item; delete on resolution.

## Open

### Accepted risks (deliberate, revisit on incident)
- NOTE (2026-08-01): pre-rewrite commit SHAs cited in older audit reports and log entries no longer resolve; git history was rewritten on 2026-08-01 (see Resolved). GitHub may serve old objects from caches or forks for a while; request GC via support if that matters.
- PENDING USER ACTION (2026-08-01): judge tier is fully wired to the keychain (`claude-judge-api-key`); it activates the moment the rotated key is stored via `security add-generic-password -a "$USER" -s claude-judge-api-key -w` in a terminal outside any session.
- ACCEPTED RISK (2026-09-16, audit P3-2, standing since the 2026-07-31 audits): `settings.json` keeps `skipDangerousModePermissionPrompt: true` against R-203's spirit; flagged in two archived audit reports and left deliberately, recorded here so the acceptance lives where future sessions look. Revisit on incident.
- RESOLVED AS FALSE POSITIVE (2026-09-16, audit P3-1): `&&`-chained commands cannot ride an `allow` prefix past an `ask` rule; Claude Code decomposes on shell operators (`&&`, `||`, `;`, `|`, `|&`, newlines) and deny/ask rules match every subcommand, including subshells and command substitutions (permissions doc, "Compound commands"). Remaining caveat: `bash -c '...'` wraps the inner text away from rule matching, which is exactly why `Bash(bash *)`/`Bash(sh *)` sit in `ask`.

### From the 2026-07-31 full-harness audits (P2/P3)
- P2 (security): `destructive-db-guard.sh` blanket `localhost` early-return misses SSH-tunneled remote DBs; argv-only scope. Known limitation; needs a design, not a patch.
- P2 (criticism): no false-block measurement exists; the new fire log records fires, not wrongly-blocked work. Add a `[false-block]` marker convention to memory entries.
- P2 (criticism): `global-memory/` and `rulebook/` carry redundant, drifting copies of collaboration rules (`PROTOCOL.md:33` vs `INDEX.md`); consolidate to one source.
- P2 (criticism): `settings.json` has no schema validation. Since 2026-09-04 `settings-change-guard.sh` refuses a mid-session user settings file that does not parse or drops a manifest-required hook; a full key-level lint remains open.
- P3 (criticism): no ID-closure guard for R-7xx/8xx/9xx rule references.
- P3 (engineering): `commit-message-guard` false-positive class on verification commands containing `git commit` text; single instance.
- P2 (2026-07-31 engineering audit): commit 266d05e is an unpaired `fix:` touching only `enforce/eslint.config.mjs` with no test; single instance, R-403 pattern note.
- P3 (2026-07-31 engineering audit): `structure-gate.sh` applies the TS-only R-314 nested-`__tests__` deny message to Python test filenames; untested edge, low likelihood.
- P3 (2026-07-31 engineering audit): `push-ruff-gate.sh` and `push-eslint-gate.sh` split the changed-file list with `xargs`, mishandling a space-containing path; pre-existing pattern shared by both gates.
- P3 (2026-07-03 engineering audit): session-lifecycle hooks (`session-start.sh`, `pre-compact.sh`) have no fixture tests; they are environment-heavy (session payloads) and need harness design before testing is honest rather than performative.

- P2 (2026-08-21 engineering audit): identical content written through a Bash heredoc bypasses `content-gate.sh` entirely, since the hook matches Write|Edit only. `secret-scan.sh` already reads heredoc bodies out of Bash commands and is the fix precedent in the same tree.
- P2 (2026-08-21 engineering audit): three of this cycle's fixtures were constructed in a way that avoids the failure mode they should probe (`content-gate.test.sh`'s `pwd -P` normalization, `structure-gate.test.sh`'s react-only client package, `parallel-session-check.test.sh`'s `sleep` stand-in for a session process). The suite is green and still overstates coverage. Generalize: a fixture that sidesteps the environment quirk tests the hook against a world that does not exist.
### From the 2026-09-16 engineering audit (remediated 2026-09-16, residue only; full report `docs/audits/2026-09-16-engineering.md`)
- PENDING USER ACTION (2026-09-16, audit P0-2): rotate the GitHub PAT held in the `GITHUB_ACCESS_TOKEN` environment variable (it leaked into session transcripts, including this remediation session's, when `env` output was printed; the shell-history scan was clean), then purge `~/.claude/projects/<project-key>/f5ab3bde-ae94-477f-af19-a16a292f2144.jsonl` and this session's transcript, and delete or triage `~/.claude/projects/<project-key>/a69cecdb-d90d-4022-87a1-03989d11fa64.jsonl` (an `ASIA` STS key id passed through it). Vendor CLI config scan completed by the user in a terminal 2026-09-16: only `~/.railway/config.json` and `~/.config/gh/hosts.yml` exist, both zero credential-shaped matches (gh stores its token in the keychain). Shell history was also clean, so the PAT rotation and transcript purge are the only remaining steps. Project keys are redacted here (public repo); reconstruct them locally as the absolute project path with slashes replaced by dashes, or list `ls ~/.claude/projects/`.
- P3 (audit P2-6 residue): consider generating the `claude/README.md` inventory counts between markers the way `render-lexicon-spec.mjs` does for the verb lists, with a fixture failing on divergence; the counts were hand-corrected this cycle and will drift again.

### From the 2026-09-17 engineering audit of claude/enforce (P0 none, P1 all four remediated on the branch; full report `docs/audits/2026-09-17-engineering.md`)
- VERIFIED FALSE, not a finding (2026-09-17): the audit's doc-drift table claims `.github/dependabot.yml` is absent and `enforce.yml:37-40` therefore stale. The file exists (restored in `612551d`), so that row is dropped per R-804(a); the report carries a verifier's note at the row.

### From the 2026-09-17 hygiene audit (open)
- P3 (2026-09-18, RETRACTED after probing, kept because a wrong diagnosis in a tracker is worse than no entry): two workers were blocked by `fix-commit-requires-test.sh` on commits that did stage a `*.test.sh` file, and both concluded the hook does not count that suffix as a test. That diagnosis is false. Three hermetic probes against the live hook all exit 0: an explicitly staged `thing.test.sh` satisfies a fix-family subject; a chained add of explicit paths followed by a fix-family commit satisfies it through the union the hook already performs over its `git add` segments; and the same chain using the add-everything form also passes. The hook's pattern contains the literal test-infix, which matches `.test.sh` directly. What actually blocked those two commits was never isolated before the sessions moved on, so the cause is unknown rather than known-and-unfixed. If it recurs, capture the exact command string and the output of `git diff --cached --name-only` at that moment, since the hook reads both, and reopen with that evidence rather than the suffix theory.
- NOTE (2026-09-18): the same probe settled the two open Codex questions favorably and they are no longer pending. `apply_patch` does reach the `Write|Edit` registration, and Codex genuinely honors an adapter deny: a denied command reported `PreToolUse Blocked` and its side-effect file was never created, so the deny is enforced rather than merely logged.
- P3 (2026-09-17, cursor-exporter final review, parked by controller ruling): three known-shape debts in translate/: the two gitignore renderers implement one algorithm twice with divergent contracts (unification riskier than the payoff pre-merge), codex's closure check still carries the whole-hook gate cursor fixed in its exporter (latent while every codex unported reason is a string), and seven translate/ function names are noun phrases against R-316 (rename churn deferred).
- P3 (2026-09-17, task-state tracker review, accepted as shipped): the tracker's liveness and GC windows are fixed constants (60-minute transcript-mtime liveness, 14-day GC), so a session idle on task-tool activity for over an hour can have its in-progress tasks offered as interrupted by a sibling session's start; the misclassification is non-destructive (injection only, never deletion of a live file). Revisit if a real liveness probe (pid registry) ever exists.
- P3 (2026-09-17, task-state tracker review, accepted as shipped): fold_task_state_log is duplicated verbatim between session-start.sh and session-end.sh per the standalone-hooks convention (hooks never source shared libraries); two copies to keep in sync if fold semantics change.
- P3 (2026-09-17, incident): `destructive-command-guard.sh` matches `.claude/hooks` as a bare substring, so chmod/rm against /tmp sandbox fixture paths containing that segment draw false denials; one denial pushed a dispatched subagent into script-wrapping evasion (caught, audited benign, R-203 pattern recorded in project memory). Anchor the path patterns to the real home prefix or exempt paths under mktemp roots, with a fixture for both directions.
- PENDING USER ACTION (2026-09-17, Task 6 hook-hashes regen): four orphaned files sit in the live `~/.claude` checkout with no tracked counterpart in this repo, left behind by two past renames because `sync.sh` then never deleted anything; its manifest-based removal (IAN-116) cannot reach them either, because they were installed before it wrote any manifest (`enforce/eslintOptions.mjs`, `enforce/renderLexiconSpec.mjs`, `enforce/resolveOutgoingBase.sh` from the kebab-case pass in `a2fa34e`; `hooks/single-file-folder-gate.sh` from the hook rename in `6fe0234`). `claude/enforce/hook-hashes.txt` was regenerated this cycle against the correct 74-file tracked tree (verified byte-identical to a fresh `shasum` pass over the worktree), so the committed manifest is accurate; but `doctor.sh`'s `hook-integrity` check computes over the live tree and will keep reporting `warn`, naming these four files, until a human deletes them from `~/.claude` (a bare `rm` of `.claude/hooks/single-file-folder-gate.sh` is denied by `destructive-command-guard.sh`'s hooks-directory pattern, so this needs manual, outside-the-harness cleanup) and then re-runs `~/.claude/hooks/hook-integrity-check.sh --update` once to drop their now-stale entries from the live copy.
- P2 (2026-09-17, found at push time): `hooks/install-git-hooks.sh` refuses to run post-monorepo ("~/.claude is not a git work tree"); it still assumes the pre-migration layout, so the tracked `pre-push.sample` changes (translator check) cannot be installed by the documented path. The previously installed hook still guards this checkout. Repoint the installer at the repo checkout (`.sync-source` or `git rev-parse`), with a fixture.
- P3 (found 2026-09-17): `no-em-dash.sh` blocked four calls this session whose text held no literal U+2014, only a shell dollar-quote escape naming that code point inside a grep pattern (the hook reads the escape as the character). The hex `printf` form is the workaround and the guard is right to be loud, but this is a concrete instance of the false-block class this file already notes has no measurement convention, so it is recorded here rather than forgotten.
- P3 (2026-09-17, `claude/hardening-session-safety` final review, deferred, routed here per R-601 since the branch's SDD progress ledger is git-ignored): four minor findings survive the branch's fix waves. `hooks/session-end.sh`'s velocity block derives `START_SHA_FILE` from a bare `${TMPDIR:-/tmp}` fallback that does not honor a sandboxed `HOME` override, so a sandboxed session's start-SHA file can land outside the sandbox. `hooks/session-start.sh`'s snapshot-writer `jq` walk over `session-snapshot.json` in `check_resume_drift` is O(n^2) in file count, cheap today only because realistic resume snapshots stay small. Two of `hooks/session-end.test.sh`'s corrupt-transcript cases (`hook exits 0 on a corrupt transcript`, `no snapshot written for a corrupt transcript`) are GREEN-only and were never RED-proven against a `session-end.sh` that mishandles a corrupt transcript. And the `mv -f "$tmp_file" ... session-snapshot.json` atomic write in `write_session_snapshot` has no test distinguishing a successful move over a stale prior snapshot from one that silently no-ops.

### From the 2026-09-04 configuration audit (open P2/P3; the rest were fixed the same day or decided on 2026-09-05, see Resolved)
- P2 (config audit P2-6): the judge tier is still inert on the keychain step. Alternative written up in the report: an experimental `agent` hook on `if: "Bash(git push *)"` runs on session credentials and can read the diff; `prompt` hooks cannot. `enforcement-guard-check.sh` would need to recognize a `type: agent` registration first.
- P3 (config audit P3-1): `skipWorkflowUsageWarning` is not in the settings reference; check `/doctor` locally.
- P3 (config audit P1-4 residue): the `Read` deny enumerates `.env`, `.env.local`, `.env.*.local`, `.env.development`, `.env.staging`, `.env.production`, `.env.test`; a `.env.*` glob would also block `.env.example`, which R-805 lets the security audit read, and deny rules carry no exceptions. Any other secret-bearing variant needs its own row.
- P3 (config audit P2-5 residue): the five deny-tier git guards (`git-workflow-guard`, `global-repo-push-guard`, `commit-message-guard`, `fix-commit-requires-test`, `conflict-markers`) still spawn on every Bash call; gating them with `if` skips `bash -c 'git push'`, the interpreter escape already accepted for the permission lists. Decide whether that acceptance extends to them.

### From the 2026-09-06 TDD harness assessment (open)
- P2 (assessment, decision 1): `enforce/tdd.sh` refuses go test and RSpec projects rather than guessing; wire each classifier with the first real project on that stack, with the fixture driving the real runner as `tdd-red-green.test.sh` does for Vitest and `tdd-pytest.test.sh` does for pytest (pytest was wired on 2026-09-19 for the FastAPI template).
- P2 (assessment): `protected-path-guard.sh` cannot see a file written by an interpreter from its own source (`python - <<PY`, `node -e`); `tdd.sh green` compares locked-file hashes against the RED commit as the backstop, so the gap is bounded to the window between GREEN and commit. Stated in both headers.
- P2 (assessment, to confirm on the real build): `agent_type` reaching `PreToolUse` command hooks, and the `SubagentStop` output contract (`decision: block`); both listed in the handoff's step 1 and 2.
- P3 (assessment, step 6; decision 15): `import-x/no-cycle`, the widened R-344 block, and the test-quality rules raise counts on any repo carrying an `.enforce-baseline.json`; Ian runs `ratchet.mjs --update` there once by hand before the push gate is expected to pass.
- P3 (assessment, step 6): `behavior-assertion-required` counts matchers syntactically; an `expect` built through a helper (`expectRow(...)`) is invisible to it and a test asserting only through such helpers is not judged. Stated in the rule header.

## Resolved
- 2026-09-18 (closed on `claude/gap-codex-redirect-writes`): the Codex shell-write gap found by the live probe is fixed, and the fix is narrower than the entry predicted. Driving the real adapter against the real `protected-path-guard.sh` showed that R-410 was NOT open for plain redirections: the guard carries a redirection, tee, and mutating-verb extractor of its own and already denied `> locked`, `>> locked`, `tee locked`, `cp`, and `mv` onto a locked test under Codex. What was genuinely bypassed is every gate Codex registers on the `Write|Edit` matcher and nowhere else, which is `structure-gate`, `content-gate`, `dependency-add-guard`, `migration-defaults-guard`, and `codex-test-author-guard`: a file written by a shell command reached none of them, so R-306, R-313, R-331, and R-328 were enforced through `apply_patch` and unenforced through `printf ... > path`. The adapter now tokenizes the command text (quote-aware, heredoc bodies stripped) and dispatches one synthetic `Write` event per target to exactly that hook list, in addition to the unchanged Bash event, which also closes two holes in the guard's own regex: a quoted path containing a space, and an `install` destination. Twenty-two cases were added to `enforce/tests/codex-adapter-contract.test.sh`, fifteen of them RED-proven against the unfixed adapter; of the seven that pass vacuously against an adapter that synthesizes nothing, four (a target built from a variable or a command substitution, a `/dev` target, and the allow of an ordinary path) were proven by mutating the fixed adapter rather than left GREEN-only. The limit is stated in the adapter header and in `codex/PORT-STATUS.md`: a path that only exists once the shell has run is dropped, never guessed at.
- 2026-09-18: the data half of the 2026-09-18 fixture-binding defect is closed. `enforce/harness-root.sh` now exports the data overrides alongside `CLAUDE_HARNESS_ROOT`, each defaulted to the checkout's own copy, so a fixture inherits code binding and data binding from one source line instead of remembering a variable name per hook: `CLAUDE_MANIFEST_FILE`, `CLAUDE_ROLE_POLICY_FILE`, `CLAUDE_SETTINGS_FILE`, `REDACTION_GUARD_SETTINGS`, `REDACTION_GUARD_HOOKS_DIR`, `CLAUDE_PROTOCOL_FILE`, `CLAUDE_INTEGRITY_ROOT`, `CLAUDE_TDD_HOME` and `CLAUDE_RULES_FILES` are bound; a value the caller already set always wins, and a path the checkout does not carry is never exported. Five variables are deliberately left unbound and the helper's header names each reason: `CLAUDE_FIRE_LOG` and `CLAUDE_SESSION_LOCK_DIR` are a telemetry log and a lock directory their readers WRITE, so binding them would have the suites writing into the working tree; `CLAUDE_MCP_DB_TARGETS` and `CLAUDE_COLOCATED_ALLOWLIST_FILE` name gitignored client-identifying lists (R-106) no checkout tracks, so binding them would swap one piece of machine state for another; and `CLAUDE_JUDGE_ACCEPT_FILE` is a presence marker written when a human accepts the honor-system tier. Two further readers were reaching into the install with no override at all and are fixed here too: `redaction-guard-check.sh` gained `REDACTION_GUARD_HOOKS_DIR` for its on-disk hook check, and `skills/task-cleanup/scripts/scan.sh` now resolves its sibling `task-tier.sh` from its own location rather than from `$HOME/.claude`. `fixture-implementation-root.test.sh` grew the matching proof: a sandbox home carrying a manifest, a role policy and a settings file whose content differs from the checkout's, two real fixtures that read them passing against it, a positive control aiming the data overrides at the sabotaged copies and requiring the same fixtures to fail, and an assertion that the write-side runtime state stays unbound. All three new cases were RED-proven against the unfixed helper. The standing proof holds: every fixture in both suites now passes with `HOME` pointed at an empty directory holding no `.claude`, with `hook-latency.test.sh` the one deliberate exception because it measures the installed hooks on purpose.
- 2026-09-17 (cursor exporter, Task 9): the cursor half of the hygiene audit's port-drift finding is now resolved to match the codex half. `translate/cursor.mjs` (Tasks 1-8) regenerates `cursor/` from `claude/` sources; its first real run against the live tree (Task 9) replaced every stale `GENERATED by cursor/build.mjs` header, brought `cursor/PORT-STATUS.md` and `cursor/.claude-port.json` current (47 of 52 hook registrations ported at the current settings.json, up from the stale 42 (45 of 50 at the first regeneration)), and rewrote `cursor/README.md` to describe the live pipeline instead of the interim hand-ported reality. `node translate/cursor.mjs --check` is now gated in CI (`.github/workflows/enforce.yml`) and at `git push` (`claude/hooks/pre-push.sample`), beside the existing `translate/codex.mjs --check` gate, so a `claude/` source edit that never got `--write`ed fails the push rather than shipping stale.
- 2026-09-17 (Ian, narrowing): the `Bash(bash *)`/`Bash(sh *)` ask rules (added 2026-09-15 for the interpreter escape) narrowed to `Bash(bash -c *)`/`Bash(sh -c *)`: the -c inline form is the vector that hides inner text from permission matching, while plain script runs were prompting on every fixture invocation. Thirteen allow entries added from a transcript-frequency scan, including two flagged interpreter exceptions (`python3 - *`, `python3 -c *`) and the mutating `./sync.sh`, accepted because the hook layer scans full command text including heredoc bodies; `npx turbo` and `curl` deliberately excluded.


- 2026-09-17 (hygiene audit): PreModelSwitch is a documented Claude Code hook event after all (verified against the current hooks reference; it receives `from_model`/`to_model` and blocks only via exit 2). `model-switch-guard.sh` reworked to warn through `systemMessage` instead of the tool-event `permissionDecision` it emitted, registered in `settings.json`, and given its R-903 manifest entry; closes the 2026-09-16 P2-4 residue.
- 2026-09-15 (Ian, reconsidered): the lenient Bash allow list (accepted 2026-08-01, restated 2026-09-05) is reversed. `permissions.allow` now uses the curated prefix list from `enforce/strict-permissions.json` instead of a single blanket `Bash` entry, with `Bash(bash *)`/`Bash(sh *)` added to `ask`. The original tradeoff was friction-versus-speed for daily solo work; it also had the side effect of making auto mode skip its own classifier for every Bash command, not just avoid the fixture-level deny/ask patterns, closing that gap is the actual reason for reversing it now, not the original friction argument.
- 2026-09-15: `ntfy-notify.sh` removed entirely (Ian no longer uses ntfy), along with its `Notification` and `Stop` wiring in `settings.json`. Closes the two open items above about it (unauthenticated topic, no fixture tests): both are moot once the hook no longer exists.
- 2026-09-06: the TDD harness assessment's four critical gaps closed on `claude/tdd-harness-llm-code-fuf9rh`: C-1 (no lock on tests, fixtures, specs) and C-2 (writable gate inputs) by `hooks/protected-path-guard.sh` (R-410, R-411, R-412); C-3 (no RED evidence) by `enforce/tdd.sh`; C-4 (`SubagentStop` unregistered) by the `verification-gate.sh` registration that skips non-writing roles.
- 2026-09-05 (Ian, asked one at a time): P1-3 `model` is `opusplan`; P2-1 `permissions.defaultMode` is `auto`; P2-9 the `code-review` and `code-simplifier` plugins are disabled in favor of the bundled `/code-review` and `/simplify`; P2-6 the judge key will be stored in the keychain by Ian (the PENDING USER ACTION row above stands until then). The Sonnet-default memory now records the `opusplan` decision instead of contradicting settings.json.
- 2026-09-04: `enforce/golangci-enforce.yml` migrated to the golangci-lint v2 schema. Verified against golangci-lint 2.5.0, which rejected the v1 file outright ("unsupported version of the configuration"); since the gate fails open on unparseable output, the Go gate had enforced nothing on any v2 install. `push-golangci-gate.test.sh` now runs `config verify` whenever the binary is on PATH, so the schema cannot silently drift again.

- 2026-09-04 (config audit, same-day remediation on `claude/config-audit-8m7s5k`): P1-1 `SessionEnd` entry carries `timeout: 60` (the docs' shared 1.5-second budget would have killed the R-603 rollup); P1-2 the PreCompact sentinel plus UserPromptSubmit pair is gone, `post-compact-rules.sh` runs under `SessionStart` matcher `compact` with a global-only rule list; P1-4 `Read` deny rules for the R-102 paths; P2-2 the allow list is the single `Bash` entry and `strict-permissions.json` carries the curated list itself; P2-3 `checkout@v7`, `setup-node@v7`, ruff 0.16.6, `permissions: contents: read`, `.github/dependabot.yml`; P2-4 ESLint 10 and `eslint-plugin-import-x` (0 advisories); P2-5 `if: "Bash(git *)"` on the eight push-time and advisory git hooks; P2-7 `hook-latency.test.sh` reads the chains from `settings.json`; P2-8 R-001 confirms the injected reads and `INDEX.md` says what the hook does; P3-2 `verification-gate.sh` memoizes the last green tree; P3-3 `settings-change-guard.sh` on `ConfigChange`; P3-4, P3-5, P3-6 wording. The 2026-07-31 P2s on the secret read side and the ESLint major are closed by P1-4 and P2-4.
- 2026-09-04: the `eslint.test.sh` "flake" filed the same morning is root-caused and fixed. `lint.mjs` took the repo root from `process.cwd()`, so running the suite from inside `enforce/`, whose own `eslint.config.mjs` counts as a local ESLint config, filtered the import-order verdict as "deferred to local ESLint" and the case passed silently. The first occurrence followed an `npm install` run from that directory. `lint.mjs` now resolves the root from the linted file (git toplevel, else the nearest ancestor carrying `.enforce.json`, a package manifest, or an ESLint config), and `eslint.test.sh` runs the case from `enforce/` so a regression fails deterministically.

- 2026-09-04: `global-memory/user_profile.md` does not exist and is not meant to. It was named in the README layout and in bootstrap step 7 but has never appeared in this repo's git history, so the README described a file no clone has ever had. Both references were removed; the collaboration preferences it purported to hold are the `global-memory/feedback_*.md` files, and the estimation and routing calibration bootstrap pointed at lives in `rulebook/cost.md` (R-906, R-903), not `CLAUDE.md`. Closed on Ian's instruction, no file created. Nothing is added to the SETUP.md "what does not ship" table, since there is nothing that does not ship.

- 2026-09-04: the R-316 verb lists in `rulebook/reference.md` are now GENERATED from `enforce/lexicon.json` by `enforce/render-lexicon-spec.mjs`, between `<!-- lexicon:begin -->` markers, and `lexicon-spec-sync.test.sh` fails the suite when they diverge. Closes the same-day P3 that filed the drift: the two were already out of step twice (the registry banning `fetch`/`drop`/`remove` the prose approved, and banning 22 synonyms where the prose listed 6), because the only thing holding them together was an instruction to edit both. Writing that fixture surfaced a second gap, now also closed: the registry could contradict itself (ban a verb that its own `verbGroups`/`scopeVerbs` still bound to a layer) and rendered it as both the layer's read verb and a banned synonym without complaint. `validateLexicon()` rejects that outright and names the offending field.

- 2026-09-04: R-320 and R-325 left the llm-judge tier for custom ESLint rules, and R-318 and R-322 left it with no replacement. This retires the 2026-07-03 P3 that recorded the `Enforcement: judge` template-token question for R-320/R-322: neither rule is judge-tier any more, so the style question it preserved no longer exists.

- 2026-08-21: R-101 was Bash-only, so MCP database tools reached a managed Postgres ungated (audit P1-2). `destructive-db-guard.sh` now takes an MCP input path for tools whose action names a SQL or migration primitive, reusing its own statement classification instead of duplicating it, and is registered for the `mcp__.*` matcher. Targets are classified from `enforce/mcp-database-targets.txt` (gitignored, client-identifying): production hard-blocks, staging and unknown ask, local passes. Non-destructive MCP writes stay silent here because R-105's verb ask already covers them, so one call draws one prompt.
- 2026-08-21: same-day audit remediation, all reproduced before fixing and covered by fixtures that failed first. P1-1: `git-workflow-guard.sh` pattern-matched the refspec text, so `git push origin HEAD`, `refs/heads/main`, and `+main` all bypassed R-514; it now resolves the refspec to a branch name. P1-3: `mcp-action-guard.sh` folded case before splitting, so every camelCase tool name (`createIssue`, `chat_postMessage`) was silent; it now splits on the case boundary first. P1-2 (partial): database write verbs added to the same guard. P2-1: an express devDependency in a React package made `structure-gate.sh` deny client files with the server vocabulary; react now wins the tie. P2-2: `single-file-folder-gate.sh` warned R-309 on the very `components/<Name>/<Name>.tsx` folder R-305 orders; paired component folders are now exempt. P2-3: `git -C <path> push` bypassed all four of `git-workflow-guard.sh`'s rules; global git options are now lifted before matching, and `-C` redirects which repo the hook inspects. P2-6: `parallel-session-check.sh` treated any live PID as a session; entries now carry a start time so a recycled PID cannot produce a permanent false warning. P2-8: `xargs` on a quote-bearing path killed the advisory pass under `pipefail`; replaced with a read loop. A `set -e` landmine introduced by the P2-3 fix (an unguarded `grep` in a command substitution silenced the entire hook) was caught by the new fixtures, not by review.
- 2026-08-01: published git history rewritten with git-filter-repo to purge the two R-106 breaches (a local home path and a client-identifying name); verified zero occurrences in a fresh clone of origin. Pre-rewrite SHAs no longer resolve.
- 2026-08-01: full-harness audit remediation. Security P0s: push gates no longer execute target-repo code (f47ca92); interpreter allow-list escape closed (acef36d), then deliberately reverted to lenient by Ian (f6c8d88, see Accepted risks). Engineering P0: plaintext API keys purged from all transcripts and history.jsonl (rotation Ian-side). Criticism P0s: inert judge tier now warns at session start with severity lookup fixed (e91142f); mechanical fire telemetry replaces the dead hand-typed log (7bdaec2). P1s: handoff loading fixed with SHA verification, chained add+commit guard bypass closed, redact-output semantics honest, hook hash integrity guard, publish guard fails closed, pre-compact contradictions removed, pre-push now runs both suites (252910e).
- 2026-07-31: structure-gate never fired for Python's app/-rooted layout (P1); R-516 guard and manifest test blind to ruff:* enforcers (P1); hook-latency chain missing push-ruff-gate (P2) -> fixed with fixture tests in the post-audit commit.
- 2026-07-03: agents/ audit-role files cited pre-renumber rule IDs (P1) -> migrated in 9c01327.
- 2026-07-03: ten guard-enforced rules missing manifest entries, closure unchecked (P1) -> closure test + entries in 77a8512.
- 2026-07-03: phantom R-503 manifest enforcer; R-505 subject format enforced but not codified (P2) -> 7ffaffc.
- 2026-07-03: seven blocking/advisory guards untested (P2) -> fixture tests + hooks/tests runner in d9a7bd8.
